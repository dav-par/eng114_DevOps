Vagrant.configure("2") do |config|

  config.vm.box = "ubuntu/xenial64"
  
  # add a private network between localhost and VM using an IP
  config.vm.network "private_network", ip: "192.168.10.100"

end
